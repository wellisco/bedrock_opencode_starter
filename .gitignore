# Local AWS credentials — never commit
config/aws.env
.aws/

# Local OpenCode config (copy from opencode.json.example)
opencode.json

# Local Pi config (copy from .pi/settings.json.example)
.pi/settings.json

# Editor / OS
.vscode/
.DS_Store
